# 每周腾讯文档学习心得 → 灵感库 归档提示词（定时任务 #7758992 使用）

> 本文件是定时任务 #7758992 的「权威提示词」，由 `scheduler-api.sh update --id 7758992 --prompt "$(cat 本文件)"` 注入。
> 固化在仓库内可防止沙箱重置导致提示词丢失；不写任何明文令牌，凭证依赖 `~/.github_pat` + 脚本自动登录。

---

执行每周「腾讯文档学习心得 → 灵感库」归档任务（遵循姜胡说框架：原始素材入灵感库，经用户阅读+我加工后才进 Topics）：

## 0. 环境与凭证（关键：避免孤立沙箱会话失败，切勿使用写死旧 IP）
- 进入仓库工作区：若 `/workspace/obisidian` 不存在，则 `git clone https://github.com/xyq201/obisidian.git /workspace/obisidian` 并 `cd /workspace/obisidian`。
- 运行 `bash ./sync-obsidian-kb.sh status`：脚本会自动完成 hosts 动态自愈（探测出网代理放行的 GitHub 前端 IP 并覆盖 /etc/hosts + ~/.user_hosts）+ gh 自动登录（读 `$GITHUB_PAT` 或 `~/.github_pat`）。若仍连通失败，**立即停止并报告用户，不要伪造内容**。
- 不要手动写死 `140.82.113.x` 等旧 IP，一律依赖脚本自愈。

## 1. 检索与去重
- 通过 tdocs-app MCP 的 `manage.search_file`（search_key: `"学习心得"`）获取全部 file_id。
- 与 `灵感库/腾讯文档学习心得/_已总结追踪.md` 中已登记 file_id 比对，挑出新增**原创笔记**（类型 `tencentdoc`；`smartcanvas` 脚本运行记录默认跳过，除非用户特别要求）。

## 2. 逐篇归档（每篇独立笔记，便于用户阅读后「加工这篇」）
对每篇新增原创笔记：
- `get_content` 抓取正文（**忠实原文，不二次改写**）。
- 归档为 `灵感库/腾讯文档学习心得/YYYY-MM-DD-<标题>.md`，包含：
  - YAML frontmatter：`title` / `source`=文档链接 / `file_id` / `type`=原创笔记 / `archived`=当天日期 / `tags` / `note`="腾讯文档原文归档（未加工素材）。读后对我说「加工这篇」→ 提炼简洁 md 入 Topics。"
  - 正文（保留章节结构、表格、参考链接）。
- 把该 file_id 补进 `_已总结追踪.md`（新增「第 N 批」小节：标题、链接、性质=原创笔记）。
- 严格遵守「已登记跳过、只处理新 file_id」，**严禁重复归档**。

## 3. 提交并推送
`bash ./sync-obsidian-kb.sh sync "kb: 每周归档 腾讯文档学习心得 → 灵感库 (N篇)"`（已含 hosts/gh 自愈 + commit/rebase/push）。

## 4. 删除确认（强制）
推送成功后，列出本次新增「原创笔记」的 file_id 作为**删除候选清单**，**务必先征求用户确认，绝不直接删除源文档**。

> 注意：若 tdocs MCP 或 gh 凭证在自动化环境不可用，立即报告并停止，不要猜测或伪造内容。
