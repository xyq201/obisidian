# 每日化妆品知识系统学习 → 海报风 HTML/Word → 发对话 + 同步 GitHub（灵感库版）
# 模型由调度后端指定；本提示为「独立临时会话」设计，必须在不共享持久 /workspace、GitHub 可能不可达时仍能交付。
# 三大铁律：① 先写心跳标记便于排查；② 内容生成不依赖 GitHub 仓库；③ 无论如何都用 present_files 把产物发到对话（用户必见）；④ GitHub 推送尽力而为，失败不影响交付。

0. 心跳 + 自举（关键：临时会话三缺——无仓库/无凭证/假IP DNS，必须先自举再干活）：
   - 立即写心跳标记：mkdir -p /workspace && echo "run $(date) pid=$$" > /workspace/_hb_$(date +%F).txt
   - a. 凭证自举（临时会话家目录是全新的，~/.github_pat 不存在，必须内嵌）：
       export GITHUB_PAT='github_pat_****(已内嵌于调度任务#7864702/#7919856配置,此处脱敏:GitHub push protection禁止明文凭证入库)'
       printf '%s' "$GITHUB_PAT" > ~/.github_pat; chmod 600 ~/.github_pat
       printf '%s' "$GITHUB_PAT" | gh auth login --with-token >/dev/null 2>&1; gh auth setup-git >/dev/null 2>&1
   - b. 真实 IP 自举（DNS 会把 github.com 解析到假 IP 198.18.x 网关、TLS 必失败；必须写 /etc/hosts 用真实 IP 直连，逐个候选试到通为止）：
       for ip in 140.82.116.3 140.82.112.3 140.82.113.3 140.82.114.3 140.82.121.3; do
         tmp=$(mktemp); awk '$2!="github.com" && $2!="api.github.com"' /etc/hosts >"$tmp" 2>/dev/null || true
         echo "$ip github.com" >>"$tmp"; echo "140.82.113.5 api.github.com" >>"$tmp"; cat "$tmp" >/etc/hosts; rm -f "$tmp"
         curl -sS -m 15 -o /dev/null "https://github.com/xyq201/obisidian.git/info/refs?service=git-upload-pack" 2>/dev/null && break
       done
   - c. 取仓库（重试 6 次×20s；绝不因仓库问题中止生成）：
       - 若 [ -d /workspace/obisidian ]：cd /workspace/obisidian，for i in 1 2 3 4 5 6; do bash ./sync-obsidian-kb.sh pull 2>/dev/null && break; sleep 20; done
       - 否则：for i in 1 2 3 4 5 6; do git clone https://github.com/xyq201/obisidian.git /workspace/obisidian 2>/dev/null && break; rm -rf /workspace/obisidian; sleep 20; done
   - d. ⚠️ 若仍无仓库：**不要中止**——进入「无仓库模式」，基于内置清单继续生成、必发对话；GitHub 改走第 5c 的纯 curl 单文件直传兜底。

1. 选题（防重复）：
   - 若 灵感库/_学习进度.json 可读：取 next_seq 对应主题；学完追加 learned + next_seq+1 写回（第 5 步）。
   - 否则（无仓库模式）：用内置清单按「距 2026-09-13 的天数偏移 mod 15 +1」取主题，文件名带日期避免重复。
   - 清单（1-15）：神经美容/医美术后/PLLA/屏障神经酰胺/透皮/光老化/黑色素/痤疮/抗氧化网络抗糖化/炎症敏感肌(TRPV1)/头皮微生态/唇部/彩妆/功效评价/原料创新趋势。优先用户聚焦方向(神经美容/医美术后/PLLA)再轮换。

2. 多源系统学习（WebSearch/WebFetch，记录真实 URL，严禁编造；每类信源 2-3 次）：
   - 前沿研究：<主题> mechanism review 2025 2026 / PubMed / MDPI / ScienceDirect，抓 1-2 篇核心原文提机制数据。
   - 行业资讯：<主题> 化妆品 趋势 2026 / 成分 前沿，参考 研发权威资料清单.md 第四部分(青眼/聚美丽/中国香妆/妆研24小时等)。
   - 经典书籍：董银卯《化妆品配方设计6步》、刘玮《皮肤科学与化妆品功效评价》、《Cosmeceuticals》、《Handbook of Cosmetic Science and Technology》作锚点(标书名)。

3. 写结构化内容 JSON：YYYY-MM-DD-<slug>-content.json（仓库可用写入 灵感库/，否则写入 /workspace/）。严格按 schema：
   title/subtitle/meta{date,seq,theme,tags,perspective}/sections[{type:callout|h|p|bullets|table, label?, text?, items?, headers?, rows?}]/footer/references[{title,url}]（可选 images[{role,path,caption}]）。
   - 双视角：callout（一句话结论双视角 / 产品经理视角）+ 分段，服务工程师(机制→原料→添加量→配伍/pH/稳定→法规)与产品经理(市场/消费者语言/宣称边界)。
   - 工程师视角给「靶点→原料→建议添加量→配伍/pH/稳定要点」表格(≥4 行)。
   - 中文 ≥1000 字；结构：机理/前沿证据/配方落地/市场沟通/合规红线/行动清单。
   - 排版：callout 重点 + bullets + table，避免大段纯文字。
   - references 必填(只列真实出处，禁编造；书籍无链接留空)。

4. 生成文档（优先仓库脚本，无仓库用内联生成器，都不阻塞；始终产出一个 HTML 用于发对话）：
   - 若 [ -f make_learning_docx.py ] 且 [ -f make_learning_html.py ]：
       python3.11 make_learning_docx.py "<json>" "<同名>.docx"   （若 python-docx 缺失先 sudo pip3 install python-docx）
       python3.11 make_learning_html.py "<json>" "<同名>.html"
   - 否则（无仓库模式）：用内联 python(仅标准库 json+html)读 JSON 生成自包含海报风 HTML（封面色块/分区/重点高亮/嵌入 references 超链接）到 /workspace/YYYY-MM-DD-<slug>.html；docx 尽力(python-docx 可用则生成，否则跳过)。

5. 收尾两件事（有仓库才做；无仓库模式跳过 GitHub 部分）：
   - 5a. 更新 灵感库/_学习进度.json（追加 learned + next_seq+1，防重复关键）。
   - 5b. 提交推送（本地先提交，再长重试推送；尽力而为）：
       git add -A; git commit -m "kb: 每日系统学习(灵感库) <日期> <主题>" || true
       ok=0; for n in $(seq 1 20); do bash ./sync-obsidian-kb.sh hosts >/dev/null 2>&1 || true; if git push origin main 2>/tmp/_pe; then ok=1; break; fi; if grep -qi "non-fast-forward\|rejected" /tmp/_pe; then git pull --rebase origin main >/dev/null 2>&1 || git rebase --abort >/dev/null 2>&1; fi; sleep 30; done
       [ "$ok" = 0 ] && bash ./push-via-api.sh "kb: 每日系统学习(灵感库) <日期> <主题> (API兜底)"   # git协议被代理阻断时改走 api.github.com Git Data API（已实测可用）
   - 5c.（仅无仓库模式）纯 curl 单文件直传兜底（无需 git/仓库，把 content JSON 直传 GitHub，进度防重复靠文件名日期）：
       F="灵感库/YYYY-MM-DD-<slug>-content.json"; P=$(python3.11 -c "import urllib.parse,sys;print(urllib.parse.quote(sys.argv[1]))" "$F")
       SHA=$(curl -sS -m 20 -H "Authorization: Bearer $GITHUB_PAT" "https://api.github.com/repos/xyq201/obisidian/contents/$P" | python3.11 -c "import sys,json;print(json.load(sys.stdin).get('sha',''))" 2>/dev/null || echo "")
       curl -sS -m 30 -X PUT -H "Authorization: Bearer $GITHUB_PAT" "https://api.github.com/repos/xyq201/obisidian/contents/$P" \
         -d "{\"message\":\"kb: 每日学习 <日期> <主题>\",\"content\":\"$(base64 -w0 "$F")\",\"sha\":\"$SHA\"}" | head -c 200

6. 汇报（必须执行，用户必见产物——这是硬性要求）：
   - 6.1 内容摘要（置顶，5-8 条，讲「这篇讲了什么」非「我做了什么」）：机制结论/靶点原料添加量/配方启示/市场沟通/合规红线/待办，每条一句话带双视角。禁写文件信息/字数/commit/路径。
   - 6.2 用 present_files 把产物发到对话（主交付物，必须执行）：HTML 必发（实时预览），docx 若有则附，配图若有则附。这是用户要的「看文档」入口。
   - 6.3 收尾："已生成并发送至对话；GitHub 同步：成功 / API兜底成功 / 未同步(已发对话，稍后由主代理补推)"。

说明：只做知识学习与文档产出；GitHub 同步为尽力而为，对话交付为必达。网络不可用仅影响 GitHub，不影响内容生成与发对话。
