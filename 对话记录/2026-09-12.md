---
title: "对话整理 · 2026-09-12｜GitHub 知识库同步管线搭建"
date: 2026-09-12
tags: [workbuddy, daily-conversation, 知识库同步]
aliases: []
source: conversation
session: "codebuddy-20260912"
created: 2026-09-12T17:34:00+08:00
---

## 主题概览
搭建「GitHub 知识库 ↔ 腾讯文档学习心得 ↔ 每日对话」的自动同步管线，并完成首次真实同步。

## 关键决策（用户拍板）
1. **认证方式**：fine-grained PAT（`gh auth login --with-token`）。
2. **自动化落地**：WorkBuddy 定时任务（本环境用 automation-task-manager 建 daily/weekly 调度；另给可移植脚本塞进 WorkBuddy 应用）。
3. **删除范围**：仅删已成功上传的腾讯文档源（先 dry-run 清单 → 用户确认 → 再删）。

## 踩坑与根因
- **GitHub 连不上**：`api.github.com` 被沙箱 DNS 污染到 `198.18.0.18`（测试网段 sink），直连 TLS 失败。根因是 DNS 污染，不是 token 问题。
- **修复**：把 GitHub 真实 IP 写入 `/etc/hosts` 绕过污染 → `gh`/`git` 恢复正常。⚠️ 该 hosts 修改在独立自动化会话中可能不持久，需在调度脚本里加一步写 hosts 兜底。

## 已落地内容
- 接入仓库 `xyq201/obisidian`（现成 Obsidian 中文知识库，已有一套学习心得汇总 + 去重追踪机制 `_已总结追踪.md`）。
- 融入现有 vault 规范：扩展 `学习心得/腾讯文档学习心得汇总.md`（第二批 2026-08-26~09-03，共 17 篇原创笔记），更新 `_已总结追踪.md` 去重表，避免重复处理/误删。
- 本对话笔记归入 `对话记录/`（每日对话整理目录，本次为第一篇）。

## 待办 / 下一步
- [ ] 用户确认后删除本次新上传的 16 篇腾讯文档源（见删除清单）。
- [ ] 建两个定时任务：每周日 23:00 学习心得归档；每日 23:30 对话整理提醒+触发。
- [ ] 验证每周任务在自动化会话中能否拿到 tdocs MCP + GitHub 凭证（含 /etc/hosts 修复）。
- [ ] 可选：把「防晒剂光稳定 / 抗氧化网络 / 痤疮免疫代谢轴 / 分区美白护理」拆为 Topics 卡片。

> 声明：本笔记由 WorkBuddy/CodeBuddy 依会话自动整理，仅供内部记录。
